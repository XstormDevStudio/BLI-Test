using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;
using UnityEngine.Serialization;

public class SaveIndividulObj : MonoBehaviour
{
    private void OnEnable()
    {
        //if (PlayerPrefs.HasKey(transform.gameObject.name))
        //{
        //    PlayerPrefs.SetString(transform.gameObject.name + FindObjectOfType<MakeObj>().all.Count, transform.position.ToString());
        //    Debug.Log("hehre");
        //}
        //else
        //{

        //    PlayerPrefs.SetString(transform.gameObject.name + FindObjectOfType<MakeObj>().all.Count, transform.position.ToString());
        //}


        //ObjectData newObj = new ObjectData();
        //newObj.x = transform.position.x.ToString();
        //newObj.y = transform.position.y.ToString();
        //newObj.z = transform.position.x.ToString();

        //MakeObj.allData.Add(newObj);





    }


}
